# Scripts históricos

Scripts de cada ronda ya ejecutados (altas, precios, fotos, correcciones). No se vuelven a correr: quedan
como prueba de dónde salió cada dato de `catalogo-final.json`. Se movieron aquí el 15-09-2026; algunas
rutas internas (`AQUI`, `os.path.dirname(__file__)`) apuntaban a `scripts/` y ya no son válidas.

Los que siguen en uso están en `scripts/`: `compatibilidad.py` → `exportar_web.py`.
